<%# <%= simple_form_for @skill do |f|  %>
<%= simple_form_for(@skill, url: root_path, :method => :put, html: { class: "mb-4"}) do |f| %>
  <%= f.input :title %>
  <%= f.input :rating %>
  <%= f.submit "Enregistrer", class: "btn btn-primary" %>
<% end %>
